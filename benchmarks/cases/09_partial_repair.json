{
  "name": "partial_repair_mixed_fixable_and_unfixable",
  "description": "One missing field has a plausible fuzzy candidate (fixable); a sibling missing field has no candidate, no alias, and no default at all (unfixable). Proves StateGuard correctly reports PARTIAL rather than silently fabricating a value or failing the entire repair.",
  "expected_schema": {
    "fields": [
      {"path": "temperature", "type": "float"},
      {"path": "humidity", "type": "integer"},
      {"path": "country_code", "type": "string"}
    ]
  },
  "broken_payload": {
    "temp_celsius": 31.5,
    "humidity": 80
  },
  "expected_result": {
    "status": "partial"
  }
}
