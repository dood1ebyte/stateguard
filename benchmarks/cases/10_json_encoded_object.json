{
  "name": "json_encoded_object_and_array",
  "description": "A model returned structured fields as JSON text instead of as objects. Parsing recovers the intended structure exactly; the array case is the one that used to be silently corrupted by wrap-in-list, which produced a valid-looking list containing the raw JSON string.",
  "expected_schema": {
    "fields": [
      {"path": "metadata", "type": "object"},
      {"path": "tags", "type": "array", "item_type": "string"}
    ]
  },
  "broken_payload": {
    "metadata": "{\"source\": \"sensor-7\", \"revision\": 3}",
    "tags": "[\"indoor\", \"calibrated\"]"
  },
  "expected_result": {
    "status": "success",
    "min_confidence": 0.9
  }
}
